# Owner rejection — rebuild directive (2026-09-16)

The current Phase C product path is rejected. This is not a request for incremental patching.

## Hard stop
- No paid GPT2/OpenAI image calls.
- Do not use the remaining historical allowance.
- Do not present the current procedural/worksheet-style pages as a product.
- Do not ask the Owner to approve more image spend until the page architecture itself is commercially convincing at zero additional paid-image cost.

## What failed
The factory optimized for technical validity and slot-filling instead of reproducing the Owner-approved commercial page standard. The rejected output has recurring problems: weak visual hierarchy, tiny/awkward mission badges, titles fighting headers, sparse search pages, maze pages that look pasted-on, answer keys that look like engineering output, and inconsistent integration between artwork and activity mechanics.

## Replacement architecture
Use the Owner-approved reference pages as the layout target, not the previous Phase B/Phase C procedural placeholders.

1. Coloring / mission opener
   - commercial full-page coloring scene
   - compact mission identifier only; never compete with title
   - title and scripture in a clean dedicated header zone
   - scene owns most of the page
   - one simple bottom reflection prompt

2. Search & Find
   - dense but colorable full-scene illustration
   - target legend is compact and readable
   - exactly eight code-controlled target objects
   - target objects integrated naturally into the scene, not floating engineering icons
   - answer key reuses the same scene and marks targets clearly at readable scale

3. Maze
   - maze is visually integrated into an illustrated environment/window
   - no pasted strip art
   - large child-usable path width
   - clear START/FINISH integrated into composition
   - deterministic solvability remains code-controlled

4. Faith in Action
   - generous whitespace
   - 3–4 large choice cards with simple icons
   - one writing/drawing area
   - no crowded microcopy

5. Answer key
   - large enough to use without zooming
   - one mission per page
   - search solution and maze solution both readable at print size

## Zero-cost acceptance before any new paid art
Rebuild all shared templates and render the full 48-page book with deterministic stand-in art. The full contact sheet must show consistent hierarchy, spacing, typography, and page rhythm across all eight missions. Automated collision/overflow/clip checks must pass. Human/PM review must explicitly reject any page that looks like a worksheet prototype rather than a retail children's activity book.

Only after the 48-page architecture passes this zero-cost gate may the Owner be asked whether to re-enable paid GPT2 artwork.
